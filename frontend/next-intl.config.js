/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  // The default locale (used when none is provided)
  defaultLocale: "my",

  // All supported locales
  locales: ["my", "en"],

  // Optional: path where translation messages are stored
  messagesDirectory: "./messages"
};